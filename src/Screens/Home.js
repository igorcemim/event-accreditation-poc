import React from 'react';
import Section from '../Components/Section';

const Home = (props) => {
  return (
    <Section title="Dashboard">Bem vindo ao dashboard.</Section>
  );
}

export default Home;
