import React from 'react';
import Section from '../Components/Section';

const NotFound = (props) => {
  return (
    <Section title="Página não encontrada">A página que você tentou acessar não existe.</Section>
  );
}

export default NotFound;
